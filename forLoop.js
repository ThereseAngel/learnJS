// Loops

// Initialize Value; Condition; Increment
for(let index = 1; index <= 4; index++){
    console.log('Hi Angel.', index);
}

// Initialize Value; Condition; Decrement
for(let index = 5; index >= 1; index--){
    console.log('Hi Angel.', index);
}