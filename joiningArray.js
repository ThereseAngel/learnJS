// Joining Array

// Covert array to string value
let numbers = [1,2,3,4];
let joinArray = numbers.join('-');
console.log(joinArray);

// Convert string to array
let message = 'Hi my name is Angel';
const arrayMessage = message.split(' ');
console.log(arrayMessage);