// JSON Data Format

const user = {
    name: 'Angel',
    age: 22,
};

// JSON format
const jsonData = JSON.stringify(user);

// JSON to object
console.log(JSON.parse(jsonData));

console.log(jsonData);
