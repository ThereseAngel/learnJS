// Function Expression [Could not run if console log were written before the function]

const calculateSumExp = function(number1, number2) {
    let sum = number1 + number2;
    return sum;
}

console.log(calculateSumExp(50, 60));